/* Rutas para usuarios */
// Módulos importados
const router = require('express').Router();

/*// Controlador
const users_controller = require('./../controllers/users');

// Rutas a usar
router.get('', users_controller.listar);
router.get('/:id', users_controller.listar);
router.post('', users_controller.nuevo);
router.put('', users_controller.actualizar);
router.put('/:id', users_controller.actualizar);
router.delete('', users_controller.borrar);
router.delete('/:id', users_controller.borrar);*/

module.exports = router;
